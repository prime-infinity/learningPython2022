# store long string
message = ''' this is the message
and it can be in multiple lines,
this is because of the three quotes.'''

# print(message)

# strings are also treated like list/arrays
course = "python course"
print(course[8])  # o
print(course[0:4])  # pyth

# formatted string
first = "john"
last = "smith"
msg = f'{first} {last} is a coder'
print(msg)
